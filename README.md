# Работа с массивами в C

## 📋 Задания к лаб. 11

1. Заполнение массива с консоли
2. Преобразование массива
3. Печать массива
4. (вариант 9) - Среднее арифметическое значений элементов массива за исключением нулевых элементов.

## 📋 Задания к лаб. 14

1. Заполнение массива значениями из функции Func(x)
2. Печать массива
3. Вычисление суммы элементов массива
4. (вариант 9) - Поиск максимального по модулю значения, не равного заданному А.

## 📋 Задания к лаб. 16

1. Генерация трех массивов со случайными вещественными числами.
2. (вариант 9) - Создание нового массива d, каждый элемент которого определяется по правилу di = min (ai, bi, ci) с исключением отсутствующих пар.
3. Вывод всех 4 массивов на экран.
4. Освобождение динамически выделенной памяти.

## 📋 Задания к лаб. 17

1. Реализация функций сортировки массива целых чисел по алгоритмам: пузырьковая, шейкерная, простым выбором, вставками.
2. Разработка функций формирования массива (`full_array`) и вывода на печать (`put_array`).
3. Проведение вычислительного эксперимента по оценке эффективности алгоритмов с замером времени выполнения (`clock()`).
4. Вывод исходного массива, полученного времени для каждого алгоритма и отсортированного массива.

## 📁 Структура проекта
├── .gitattributes

├── .gitignore

├── Arrays.sln

├── Arrays.vcxproj

├── Arrays.vcxproj.filters

├── README.md

├── app.c

├── array.h

└── arrays.c

## 🛠️ Реализованные функции

# **Для лабораторной №14:**
### 1. `double* full_elements(double* array_14, int size)`
- **Назначение**: Заполнение массива значениями через функцию func(x)
- **Параметры**: 
  - `array_14` - указатель на массив
  - `size` - размер массива
- **Возвращает**: указатель на заполненный массив
  
### 2. `void print_array(double* array_14, int size)`
- **Назначение**: Вывод элементов массива на экран
- **Параметры**: 
  - `array_14` - указатель на массив
  - `size` - количество элементов для вывода
  
### 3. `double sum_array(double* array_14, int size)`
- **Назначение**: Вычисление суммы всех элементов массива
- **Параметры**: 
  - `array_14` - указатель на массив
  - `size` - количество элементов
- **Возвращает**: сумму элементов типа double
  
### 4. `double find_max_not_A(double* array_14, int size, double A)`
- **Назначение**: Поиск максимального значения, не равного А
- **Параметры**: 
  - `array_14` - указатель на массив
  - `size` - размер массива
  - `A` - значение для исключения их поиска
- **Возвращает**: максимальное значение, не равное А


# **Для лабораторной №16:**
### 1. `double* create_random_array(int size)`
- **Назначение**: Создание динамического массива со случайными числами
- **Параметры**: 
  - `size` - размер создаваемого массива
- **Возвращает**: указатель на динамически выделенный массив
  
### 2. `double find_min_among_three(double a, double b, double c, int* valid_count)`
- **Назначение**: Нахождение минимального значения среди трех чисел
- **Параметры**: 
  - `a, b, c` - сравниваемые значения
  - `valid_count` - указатель на счетчик валидных значений
- **Возвращает**: минимальное значение среди трех
  
### 3. `double* create_d_array_min(double* a, int size_a, double* b, int size_b, double* c, int size_c, int* result_size)`
- **Назначение**: Создание массива по правилу d[i] = min(a[i], b[i], c[i]).
- **Параметры**: 
  - `a, b, c` - указатели на исходные массивы
  - `size_a, size_b, size_c` - размеры исходных массивов
  - `result_size` - указатель для возврата размера результатирующего массива
- **Возвращает**: указатель на новый массив d
  
### 4. `void print_array_info(double* array, int size, const char* name)`
- **Назначение**: Вывод информации о массиве с указанием его имени
- **Параметры**: 
  - `array` - указатель на массив
  - `size` - размер массива
  - `name` - имя массива для отображения
 
# **Для лабораторной №17:**
### 1. `void sort_bubble(int* ptrarr, int n)`
- **Назначение**: Сортировка массива методом пузырька
- **Параметры**: 
  - `ptrarr` - указатель на сортируемый массив
  - `n` - количество элементов

### 2. `void sort_kokteil(int* ptrarr, int n)`
- **Назначение**: Сортировка массива шейкерным методом (перемешиванием)
- **Параметры**: 
  - `ptrarr` - указатель на сортируемый массив
  - `n` - количество элементов

### 3. `void sort_select(int* ptrarr, int n)`
- **Назначение**: Сортировка массива методом простого выбора
- **Параметры**: 
  - `ptrarr` - указатель на сортируемый массив
  - `n` - количество элементов

### 4. `void sort_insert(int* ptrarr, int n)`
- **Назначение**: Сортировка массива методом вставок
- **Параметры**: 
  - `ptrarr` - указатель на сортируемый массив
  - `n` - количество элементов

### 5. `int* full_array(int* ptrarr, int n)`
- **Назначение**: Заполнение массива случайными целыми числами
- **Параметры**: 
  - `ptrarr` - указатель на массив
  - `n` - размер массива
- **Возвращает**: указатель на заполненный массив

### 6. `int put_array(int* ptrarr, int n)`
- **Назначение**: Вывод элементов целочисленного массива на экран
- **Параметры**: 
  - `ptrarr` - указатель на массив
  - `n` - размер массива
- **Возвращает**: количество выведенных элементов

### 7. `void copy_int_array(const int* src, int* dest, int n)`
- **Назначение**: Копирование содержимого одного массива в другой (для восстановления исходных данных перед тестами)
- **Параметры**: 
  - `src` - указатель на исходный массив
  - `dest` - указатель на массив-приемник
  - `n` - количество копируемых элементов
